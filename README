# Start the docker containers
`docker-compose up`
Wait a minute or two after the images start to form the cluster and allow Kibana to connect

# Access Kibana
http://localhost:5601

# Access Node1
http://localhost:9200

# Access Node2
http://localhost:9201